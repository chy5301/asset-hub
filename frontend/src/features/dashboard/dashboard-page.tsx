/**
 * Dashboard page placeholder.
 *
 * 真正的看板内容由 Task 11 (D-原版 grid + atmosphere) + Task 12-15 落地。
 * 此占位让 Task 8 的 route 可单独 import + 编译通过。
 */
export function DashboardPage() {
  return <div>Dashboard 占位</div>;
}
