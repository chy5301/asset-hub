# v2.x · serve start IPv6 端口探测 + Vite strictPort · 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修 `serve start` 不识别外部进程占用 5173 的 IPv6-only listener 漏检 bug，并加 Vite `strictPort` 兜底。

**Architecture:** 单点修 `proc.is_port_in_use` 改双栈探测（IPv4 + IPv6），并在 `vite.config.ts` 加 `strictPort` 兜底。不动 PID 文件 schema、不引入 stderr 解析。TDD：先写 IPv6 占用测试看到 fail，再改 `is_port_in_use` 实现。

**Tech Stack:** Python `socket` 模块（AF_INET / AF_INET6 双 family）、Vite 8.x `server.strictPort`。

**Spec:** [`docs/superpowers/specs/2026-05-13-v2.x-serve-port-detection-design.md`](../specs/2026-05-13-v2.x-serve-port-detection-design.md)

---

## 任务总览

| Task | 主题 | 产物 |
|---|---|---|
| 1 | commit docs (spec + plan) | git commit |
| 2 | TDD 加 IPv6 测试 → 验证 fail → 修实现 → 测试 pass → commit | proc.py + test_proc_port_check.py + git commit |
| 3 | vite.config.ts 加 strictPort + commit | frontend/vite.config.ts + git commit |
| 4 | 全量验证（pytest / ruff / pnpm test）+ 登记 followup commit | followup-allocation.md + git commit |
| 5 | push + 创建 PR | remote 分支 + PR # |
| 6 | 跟 CI 实战、merge、ExitWorktree | PR merge |

---

### Task 1: commit docs（spec + plan）

**Files:** 已存在（worktree 入口时写好）
- `docs/superpowers/specs/2026-05-13-v2.x-serve-port-detection-design.md`
- `docs/superpowers/plans/2026-05-13-v2.x-serve-port-detection.md`

- [ ] **Step 1: 检查 git status**

Run: `git status`
Expected: 看到 2 个 untracked docs 文件

- [ ] **Step 2: commit**

```bash
git add docs/superpowers/specs/2026-05-13-v2.x-serve-port-detection-design.md \
        docs/superpowers/plans/2026-05-13-v2.x-serve-port-detection.md
git commit -m "docs(v2.x): PR-C serve port detection spec + plan"
```

---

### Task 2: TDD 修 is_port_in_use 双栈

**目的**：先写 IPv6 占用测试 → 看到旧 IPv4-only 实现 fail → 改实现 → 测试 pass。

**Files:**
- Modify: `tests/unit/test_proc_port_check.py`
- Modify: `src/asset_hub/cli/serve/proc.py:85-95`

- [ ] **Step 1: 加 IPv6 占用测试（先写、应 fail）**

把以下两个测试 append 到 `tests/unit/test_proc_port_check.py` 现有 2 个 test 之后：

```python
def test_port_in_use_returns_true_for_ipv6_only():
    """IPv6 ::1 上监听的端口必须被识别为占用（Vite 8.x dev 默认只绑 IPv6）。"""
    s = socket.socket(socket.AF_INET6, socket.SOCK_STREAM)
    s.bind(("::1", 0))
    port = s.getsockname()[1]
    s.listen(1)
    try:
        assert is_port_in_use(port) is True
    finally:
        s.close()


def test_port_in_use_handles_ipv6_disabled_gracefully(monkeypatch):
    """IPv6 系统级不可用时（如某些容器），探测应优雅退化为 IPv4 单栈而非崩溃。"""
    import socket as socket_mod

    real_socket = socket_mod.socket

    def fake_socket(family, *a, **kw):
        if family == socket_mod.AF_INET6:
            raise OSError("Address family not supported by protocol")
        return real_socket(family, *a, **kw)

    monkeypatch.setattr("asset_hub.cli.serve.proc.socket.socket", fake_socket)
    # 在系统级 IPv6 不可用时，free IPv4 端口仍应识别为 free（bool 而非异常）
    s = real_socket(socket_mod.AF_INET, socket_mod.SOCK_STREAM)
    s.bind(("127.0.0.1", 0))
    free_port = s.getsockname()[1]
    s.close()
    result = is_port_in_use(free_port)
    assert isinstance(result, bool)
```

- [ ] **Step 2: 跑这 2 个新测试看 fail**

Run: `uv run pytest tests/unit/test_proc_port_check.py::test_port_in_use_returns_true_for_ipv6_only tests/unit/test_proc_port_check.py::test_port_in_use_handles_ipv6_disabled_gracefully -v`

Expected：
- `test_port_in_use_returns_true_for_ipv6_only` **FAIL**（旧 IPv4-only 实现漏检 IPv6 占用）
- `test_port_in_use_handles_ipv6_disabled_gracefully` 可能 PASS（旧实现不碰 IPv6，自然不崩）或 FAIL，无论如何记录现状

- [ ] **Step 3: 改 `proc.py::is_port_in_use` 为双栈**

old_string（精确匹配 `src/asset_hub/cli/serve/proc.py:85-95`）：

```python
def is_port_in_use(port: int, host: str = "127.0.0.1") -> bool:
    """检查 host:port 是否被占用（true = 已占用）。"""
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.settimeout(0.5)
    try:
        s.bind((host, port))
    except OSError:
        return True
    finally:
        s.close()
    return False
```

new_string：

```python
def is_port_in_use(port: int) -> bool:
    """检查指定端口是否被占用（IPv4 127.0.0.1 或 IPv6 ::1 任一被占即 True）。

    Vite 8.x dev 默认只监听 IPv6 ::1；uvicorn 显式 --host 绑 IPv4 127.0.0.1。
    任一栈被占用都需要识别，否则会出现 "IPv4 探测显示空闲但 IPv6 已被占" 的漏检，
    导致 Vite 偷偷 fallback 到下一个端口而 PID 文件撒谎。

    系统级 IPv6 不可用（容器、特殊网络栈）时，AF_INET6 socket 创建即抛 OSError，
    捕获后视为"那一栈不占"继续 IPv4 探测——graceful degrade 不让该函数变成系统
    可用性 gate。
    """
    for family, host in (
        (socket.AF_INET, "127.0.0.1"),
        (socket.AF_INET6, "::1"),
    ):
        try:
            s = socket.socket(family, socket.SOCK_STREAM)
        except OSError:
            # 该地址族系统级不可用（如关 IPv6 的容器）→ 视为"该栈不占"继续下一栈
            continue
        s.settimeout(0.5)
        try:
            s.bind((host, port))
        except OSError:
            return True
        finally:
            s.close()
    return False
```

- [ ] **Step 4: 跑全部 proc 测试**

Run: `uv run pytest tests/unit/test_proc_port_check.py -v`
Expected: 4 个测试全 PASS（原 2 + 新 2）

- [ ] **Step 5: 跑全量 pytest 防回归**

Run: `uv run pytest`
Expected: 611 passed / 1 skipped（原 609 + 新 2）

- [ ] **Step 6: 跑 ruff check + format 确认未污染**

Run: `uv run ruff check . && uv run ruff format --check .`
Expected: All checks passed + 142 files already formatted

- [ ] **Step 7: commit**

```bash
git add src/asset_hub/cli/serve/proc.py tests/unit/test_proc_port_check.py
git commit -m "$(cat <<'EOF'
fix(serve): is_port_in_use IPv4+IPv6 双栈探测

Vite 8.x dev 默认监听 IPv6 ::1，旧 is_port_in_use 仅探 IPv4 127.0.0.1，
外部 Vite 占住 ::1:5173 时漏检 → start_service 不 fail-fast → 新 Vite
检测到 ::1:5173 占用偷偷 fallback 5174 → PID 文件/status 仍报 5173 撒谎。

修：双栈循环 bind 探测，任一栈被占即 True；IPv6 系统级不可用时
（AF_INET6 socket 创建抛 OSError）优雅退化为 IPv4 单栈。

API 微改：去掉 host 参数（仅两处调用均默认 host，删之零 caller 改动；
双栈固定后 host 参数无语义意义）。

测试：加 2 个 IPv6 场景测——::1 监听端口必须识别为占用；IPv6 系统级
不可用时 graceful 退化不崩。doctor / serve start 共享 is_port_in_use
自动受益。

PR-1 visual smoke 首次复现、PR-3 烟测第二次撞——followup-allocation.md
v2.0 PR-3 段第一条 followup。
EOF
)"
```

---

### Task 3: vite.config.ts 加 strictPort 兜底

**目的**：万一 (a) 漏（如奇特网络栈），Vite 也 fail-fast 而非偷偷 fallback。

**Files:**
- Modify: `frontend/vite.config.ts:18-26` server 段

- [ ] **Step 1: Read vite.config.ts 确认 server 段结构**

Run: Read `frontend/vite.config.ts`

- [ ] **Step 2: Edit 加 strictPort**

old_string：

```typescript
  server: {
    port: 5173,
    proxy: {
```

new_string：

```typescript
  server: {
    port: 5173,
    strictPort: true,
    proxy: {
```

- [ ] **Step 3: 本地跑前端单测确认无回归**

Run: `pnpm --dir frontend test`
Expected: 179 PASS

- [ ] **Step 4: 跑 tsc -b 确认无类型回归**

Run: `pnpm --dir frontend exec tsc -b`
Expected: 0 error

- [ ] **Step 5: 跑 ESLint**

Run: `pnpm --dir frontend lint`
Expected: 0 error（既有 2 warning 可接受，与 PR-A 验证一致）

- [ ] **Step 6: commit**

```bash
git add frontend/vite.config.ts
git commit -m "$(cat <<'EOF'
fix(frontend): vite.config strictPort 防止端口占用时静默 fallback

server.strictPort: true 让 Vite 在 5173 被占时直接 exit 非零，而非
自动 fallback 到 5174 撒谎。与 proc.is_port_in_use 双栈探测配合：
proc 探测是第一道防线，strictPort 是第二道兜底——万一探测漏（极端
网络栈），strictPort 让 start_service Phase 4 frontend probe 失败
触发 rollback，让用户看到清晰 "frontend failed to start" 而非
"已启动在 5173" 但实际 5174 的撒谎。

只影响 dev 模式（prod 不启 Vite）；e2e CI runner 上 5173 干净，
不影响 e2e 行为。
EOF
)"
```

---

### Task 4: 登记 followup + 全量验证

- [ ] **Step 1: Edit `docs/superpowers/followup-allocation.md`**

在 v2.0 PR-3 段的"未解决 followup"列表第一条 `- \`asset-hub serve start\` 不识别外部端口占用` 后追加：

```
  → ✅ PR-C (#TBD) 已修：proc.is_port_in_use IPv4+IPv6 双栈 + vite.config.ts strictPort 兜底（commit fix(serve)/fix(frontend)）
```

并在文件末尾追加新段（参考 PR-A / PR-B 模板）：

```markdown

---

## v2.x polish · serve start IPv6 端口探测 PR-C · ⏳ **等 review/merge（2026-05-13）**

PR：https://github.com/chy5301/asset-hub/pull/<TBD>
分支：`fix/v2.x-serve-port-detection`
spec / plan：`docs/superpowers/specs/2026-05-13-v2.x-serve-port-detection-design.md` + `docs/superpowers/plans/2026-05-13-v2.x-serve-port-detection.md`

落地内容：

- (a) `src/asset_hub/cli/serve/proc.py::is_port_in_use` IPv4 127.0.0.1 + IPv6 ::1 双栈探测；IPv6 系统级不可用时 graceful 退化为 IPv4 单栈；删除 `host` 参数（仅两处调用均默认 host，零 caller 改动）
- (a') `frontend/vite.config.ts` 加 `server.strictPort: true` 兜底（Vite 端口占用时直接退出非零，而非 fallback 撒谎）
- 不做 (b) doctor 占用者 PID 检测：(a) 修复后 doctor.check_port_free 自动获得双栈，无需额外项
- 不做 (c) PID 文件加 port 字段：(a)+(a') 让 fallback 路径不存在，PID 设定 port 即真实监听 port

根因（首次明确记录在 spec）：Vite 8.x dev 默认监听 IPv6 ::1（lifecycle.py 已有注释提示），原 is_port_in_use 仅探 IPv4 127.0.0.1 → 漏检 → start_service 不 fail-fast → Vite 启动后内部检测 ::1:5173 占用偷偷 fallback 5174 → PID 文件记 5173 撒谎。PR-1 visual smoke 首次复现、PR-3 烟测第二次撞，本 PR 终结。

关闭 followup：原 v2.0 PR-3 段"未解决 followup"第一条（serve port detection）由本 PR 闭环。
```

- [ ] **Step 2: 全量 pytest + ruff 收尾**

Run: `uv run pytest && uv run ruff check . && uv run ruff format --check .`
Expected: 611 PASS, lint 全绿, format 幂等

- [ ] **Step 3: 前端单测 / tsc / lint 收尾**

Run: `pnpm --dir frontend test && pnpm --dir frontend exec tsc -b && pnpm --dir frontend lint`
Expected: 179 PASS, 0 tsc error, 0 ESLint error

- [ ] **Step 4: commit followup**

```bash
git add docs/superpowers/followup-allocation.md
git commit -m "docs(followup): 登记 PR-C 落地 + 关闭 v2.0 PR-3 serve port followup"
```

---

### Task 5: push + 创建 PR

- [ ] **Step 1: 推分支**

Run: `git push -u origin fix/v2.x-serve-port-detection`

- [ ] **Step 2: gh pr create**

```bash
gh pr create --title "fix(serve): PR-C v2.x serve start IPv6 端口探测 + Vite strictPort" --body "$(cat <<'EOF'
## Summary
修 `asset-hub serve start --mode dev` 不识别外部进程占用 5173 的 bug（IPv6-only listener 漏检 + Vite 静默 fallback 双重根因）。

- (a) \`proc.is_port_in_use\` IPv4 127.0.0.1 + IPv6 ::1 双栈探测；IPv6 系统级不可用时 graceful 退化；签名简化（删 host 参数，无 caller 改动）
- (a') \`frontend/vite.config.ts\` \`server.strictPort: true\` 兜底
- v2.x polish 三 PR 系列收官；前置 PR-A (#6) + PR-B (#7) 已合入 main

## 根因（spec 首次明确记录）
Vite 8.x dev 默认监听 **IPv6 ::1**（lifecycle.py:144-146 已有注释），原 \`is_port_in_use\` 仅探 IPv4 127.0.0.1 → 漏检 → 启 Vite → Vite 内部检测 ::1:5173 占用 → 偷偷 fallback 5174 → PID 文件/status 撒谎。PR-1 visual smoke 首次撞、PR-3 烟测第二次撞，本 PR 终结。

## 不做（有意）
- 不做 (b) doctor 加"占用者 PID 是否在 serve 管理范围"检测：(a) 修复后 doctor.check_port_free 自动获得双栈，无需额外项
- 不做 (c) PID 文件加 port 字段 + 从 Vite stderr 解析：(a)+(a') 让 fallback 路径不存在，YAGNI

## 验证（本地预跑）
- [x] \`uv run pytest\`：611 PASS / 1 skipped（原 609 + 新 2 IPv6 测）
- [x] \`uv run ruff check .\`：All checks passed
- [x] \`uv run ruff format --check .\`：142 files already formatted
- [x] \`pnpm --dir frontend test\`：179 PASS
- [x] \`pnpm --dir frontend exec tsc -b\`：0 error
- [x] \`pnpm --dir frontend lint\`：0 error

## Test plan
- [ ] GitHub Actions \`ci / backend\`（pytest 611 + ruff check + ruff format check）绿
- [ ] GitHub Actions \`ci / frontend\`（ESLint + tsc -b + vitest 179）绿
- [ ] GitHub Actions \`e2e / e2e\` 绿

## 与 v2.0.1 发版关系
PR-A / PR-B 是开发者侧 infra（CI / format baseline），无 runtime 影响；PR-C 是用户可见 bug fix 向下兼容 → 三者合并 SemVer **PATCH** → **v2.0.1**。

## Spec / Plan
- spec: \`docs/superpowers/specs/2026-05-13-v2.x-serve-port-detection-design.md\`
- plan: \`docs/superpowers/plans/2026-05-13-v2.x-serve-port-detection.md\`
EOF
)"
```

---

### Task 6: 跟 CI 实战 → merge → ExitWorktree

- [ ] **Step 1: 启动 Monitor 跟 3 个 check**

参考 PR-A/PR-B 的 monitor 命令格式。

- [ ] **Step 2: 三 check 全绿后 squash merge**

```bash
gh pr merge <PR#> --squash --subject "..." --body "..."
```

- [ ] **Step 3: 若 e2e flaky 撞 playwright install timeout（PR-A 时已知）**

Run: `gh run rerun <run-id> --failed`，与 PR-B 时处理方式一致

- [ ] **Step 4: ExitWorktree**

Run: ExitWorktree(action=remove, discard_changes=true)

- [ ] **Step 5: pull main**

Run: `git checkout main && git pull --ff-only`

---

## 自检（writing-plans Self-Review）

### Spec coverage 对照

| Spec 章节 | 对应 Task |
|---|---|
| §2.1 双栈 is_port_in_use | Task 2 |
| §2.2 vite strictPort | Task 3 |
| §2.3 IPv6 测试 | Task 2 Step 1 |
| §2.4 followup-allocation 更新 | Task 4 Step 1 |
| §3.3 删 host 参数 | Task 2 Step 3 new_string |
| §5 验收标准 | Task 2 + Task 3 + Task 4 |
| §6 commit 拆分 | Task 1-4 各独立 commit |

**无遗漏。**

### Placeholder 扫描

- 无 TBD（PR# 占位在登记 followup 时会显式 search-replace；spec / plan 提到 `<TBD>` 字符串只是文档模板表达）
- 所有 expected 输出具体（"611 PASS / 1 skipped"、"179 PASS" 等）
- 完整给出 old_string / new_string

### 类型/命名一致性

- 分支名 `fix/v2.x-serve-port-detection` 始终一致
- 命令一致

无漂移。
